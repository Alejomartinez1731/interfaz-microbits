# Variables de Entorno para interfaz-microbits
# ===========================================
# Copia este archivo a .env.local para desarrollo local
# En Vercel, configura estas variables en Settings → Environment Variables

# URL del webhook de N8N (OBLIGATORIO)
# Ejemplo: https://micro-bits-n8n.aejhww.easypanel.host/webhook
N8N_WEBHOOK_URL=https://micro-bits-n8n.aejhww.easypanel.host/webhook

# NOTA: Para development local sin Vercel, necesitarás:
# 1. Configurar CORS en N8N, o
# 2. Usar un servidor proxy local (como http-proxy-middleware)
